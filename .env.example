# Rename this file to ".env" and adjust values

# Postgres in docker-compose
DATABASE_URL=postgresql://vapor:vapor@db:5432/vapor

# Client tokens (keep them secret; do not commit .env to git)
IOS_TOKEN=ios_secret_abc123
TELEGRAM_TOKEN=tg_secret_xyz456
